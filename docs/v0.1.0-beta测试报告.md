# MediaFlow v0.1.0-beta 测试报告

测试日期：2026-07-18

## 环境

### Windows

- Windows 11 家庭版 64-bit 25H2
- Flutter 3.44.6 stable
- Dart 3.12.2
- Visual Studio Community 2022 17.14.36
- Windows SDK 10.0.26100.0

### Android

- 设备：一加 13（PJZ110）
- Android 16 / API 36
- Android SDK：`D:\Android\Sdk`
- compileSdk / targetSdk：36
- minSdk：24
- USB ADB 安装与调试

## 自动化质量检查

- `dart format`：通过
- `flutter analyze`：无问题
- `flutter test`：52 项全部通过
- `flutter build windows --release`：通过
- `flutter build apk --release`：通过

测试覆盖：

- Bilibili 视频信息、标准链接、短链接、失效链接和网络超时
- 抖音分享链接重定向、Router Data、JSON-LD、空标题 fallback 和媒体地址
- HTTP 最终 URL 和重定向限制
- 下载队列、暂停、继续、失败重试和删除
- 下载连接重试、非媒体响应拒绝和文件系统失败
- HTTP Range 断点续传与 `.part` 文件
- Windows/Android 兼容文件名清理
- 下载历史、启动恢复和设置持久化

## Windows RC 验收

结果：通过。

真实数据验证：

- Bilibili `b23.tv` 短分享链接：重定向、解析、真实媒体地址和下载通过
- Bilibili 标准视频链接：解析、真实媒体地址和下载通过
- 抖音 `v.douyin.com` 短分享链接：重定向、解析、真实媒体地址和下载通过
- 文件保存到 Windows Downloads 下的 `MediaFlow` 目录
- 下载文件具有有效 MP4 结构
- Windows Media Foundation 可读取并播放测试文件
- 最新 Windows Release 启动冒烟测试通过

## Android USB 真机验收

结果：通过。

验证项目：

- Release APK 安装与应用启动
- Bilibili `b23.tv` 短分享链接
- Bilibili 标准视频链接
- 抖音 `v.douyin.com` 短分享链接
- 真实媒体文件下载
- 完成文件发布到 `/storage/emulated/0/Download/MediaFlow/`
- MediaStore 正确索引，系统文件管理器和视频播放器可访问
- 真实大文件下载暂停和继续
- 应用强制停止、重新启动后的任务状态与历史恢复
- 未完成测试任务删除后清理部分文件

真机完成文件包括 Bilibili 短链接、Bilibili 标准链接和抖音短链接测试文件。

## 发布签名状态

构建脚本在 `android/key.properties` 存在时使用 release keystore，否则回退到调试签名。公开发布资产应在配置项目私有 release keystore 后重新构建，并对最终签名 APK 再执行一次安装和启动冒烟测试。

## 发布前剩余事项

- 配置 Android 私有 release keystore
- 从当前最终提交重新生成 Windows ZIP 与 SHA256
- 生成正式签名 Android APK 与 SHA256 到 `dist`
- 对最终发布资产执行一次 Windows 启动和 Android 安装冒烟测试
- 推送、合并到 `main`、创建 Tag 和 GitHub Release